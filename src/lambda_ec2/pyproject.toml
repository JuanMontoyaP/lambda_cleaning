[project]
name = "lambda-ec2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.40.73",
]

[dependency-groups]
dev = [
    "moto[ec2]>=5.1.16",
    "pytest>=9.0.1",
]
